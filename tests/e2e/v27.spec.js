'use strict';
const {test,expect}=require('@playwright/test');const APP='/Multiverse_Wheel_V8_1326_Real_Repo_Images.html?e2e=v27';async function load(page){await page.goto(APP);await expect(page.getByRole('heading',{name:'Choose where the story begins'})).toBeVisible();await expect.poll(()=>page.evaluate(()=>game?.state?.v27?.schemaVersion)).toBe(27);}async function enter(page){await page.evaluate(()=>game.closeTitleV13(true));await expect(page.locator('#v26-world-content-beacon.v27-world-content-beacon')).toBeVisible();}async function openAtlas(page){await page.locator('[data-v26-open]').first().click();await expect(page.locator('.v27-atlas-head')).toBeVisible();await expect(page.locator('[data-v16-world-tab="atlas"]')).toHaveClass(/active/);}
test('V27 Journey 1 — Mega-Atlas exposes 1,912 assets across 25 families',async({page})=>{await load(page);await enter(page);await openAtlas(page);await expect(page.locator('.v27-atlas-head')).toContainText('1912');await expect(page.locator('.v27-atlas-head')).toContainText('25');expect(await page.locator('.v26-asset-card').count()).toBe(72);const source=await page.locator('.v26-asset-card img').first().getAttribute('src');expect(source).toMatch(/^data:image\/svg\+xml/);});
test('V27 Journey 2 — new families, groups, search, and pagination remain browsable',async({page})=>{await load(page);await enter(page);await openAtlas(page);await page.locator('[data-v27-group="world"]').click();await expect(page.locator('[data-v27-group="world"]')).toHaveClass(/active/);await page.locator('[data-v26-kind="weapon"]').click();await expect(page.locator('.v26-atlas-status')).toContainText('72 matching assets');await page.locator('[data-v26-search]').fill('rifle');expect(await page.locator('.v26-asset-card').count()).toBeGreaterThan(0);await expect(page.locator('.v26-asset-card').first()).toContainText(/Rifle/i);await page.locator('[data-v26-kind=""]').click();await page.locator('[data-v26-search]').fill('');const first=await page.locator('.v26-asset-card').first().getAttribute('data-v26-asset-card');await page.locator('[data-v27-page="1"]').first().click();const second=await page.locator('.v26-asset-card').first().getAttribute('data-v26-asset-card');expect(second).not.toBe(first);});
test('V27 Journey 3 — enriched context preserves V26 identity and adds V27 detail',async({page})=>{await load(page);await enter(page);const ids=await page.evaluate(()=>{game.ensureV27();const universe=game.state.v16.currentUniverse||game.state.customCharacter.homeworld||'Earth-Prime',legacy=game.state.v26.assignments[`world:${universe}`].assetIds.slice(),context=new MultiverseDomain.WorldContentMegaEngine().context(game.state,'world',universe,{slots:6}).map(x=>x.id);game.save();return{universe,legacy,context};});expect(ids.context.slice(0,ids.legacy.length)).toEqual(ids.legacy);expect(ids.context.length).toBe(6);expect(await page.evaluate(ids=>ids.context.some(id=>String(window.WORLD_CONTENT_CATALOG_V27.find(x=>x.id===id)?.path||'').startsWith('generated:v27/')),ids)).toBe(true);await page.evaluate(()=>game.openWorldV16('overview'));await expect(page.locator('.v27-context-strip')).toBeVisible();expect(await page.locator('.v27-context-strip article').count()).toBe(6);});
test('V27 Journey 4 — discoveries and favorites persist with no duplicate economy',async({page})=>{await load(page);await enter(page);await openAtlas(page);await page.locator('[data-v26-kind="prop"]').click();const card=page.locator('.v26-asset-card').first(),id=await card.getAttribute('data-v26-asset-card');await card.click();await card.locator('[data-v26-favorite]').click();const saved=await page.evaluate(id=>({discovered:game.state.v27.discoveries.includes(id),favorite:game.state.v26.favorites.includes(id),wallet:game.state.v27.wallet,currency:game.state.v27.currency,inventory:game.state.v27.inventory}),id);expect(saved.discovered).toBe(true);expect(saved.favorite).toBe(true);expect(saved.wallet).toBeUndefined();expect(saved.currency).toBeUndefined();expect(saved.inventory).toBeUndefined();await page.reload();await expect.poll(()=>page.evaluate(id=>game?.state?.v27?.discoveries?.includes(id)&&game?.state?.v26?.favorites?.includes(id),id)).toBe(true);});
test('V27 Journey 5 — V21–V26 surfaces still work without console errors',async({page})=>{const errors=[];page.on('console',msg=>{if(msg.type()==='error')errors.push(msg.text());});page.on('pageerror',err=>errors.push(err.message));await load(page);await enter(page);await openAtlas(page);for(const tab of ['crises','activities','operations','civilians','factions']){await page.locator(`[data-v16-world-tab="${tab}"]`).click();await expect(page.locator('.v27-context-strip')).toBeVisible();}expect(errors).toEqual([]);});
